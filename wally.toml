[package]
name = "logan/game"
version = "0.1.0"
registry = "https://github.com/UpliftGames/wally-index"
realm = "shared"

[dependencies]

[package]
name = "logan/my-game"
version = "0.1.0"
registry = "https://github.com/UpliftGames/wally-index"
realm = "shared"

[dependencies]

[server-dependencies]
# verify current version on ProfileStore's page before pinning
ProfileStore = "loleris/profilestore@<latest>"